import { Table } from "nextra/components";

# **Batches Object Overview**

## **What are Batches?**

For **high-volume projects**, **batches** help organize and manage **large-scale tasks** by dividing them into **smaller units**. Batches can:

- **Group tasks** based on **dataset types**.
- **Organize work by submission periods** (e.g., weekly submissions).
- **Track task progress** and improve **workflow efficiency**.

---

## **Batch Workflows in Skala**

### **1️⃣ Skala Enterprise Workflow**

Designed for **large-scale enterprise projects** with **ongoing task submissions**.

**Process Flow:**

1. **Batch is created** → `status = in_progress`
2. **Tasks are added to the batch** → Specify the `batch` field on the task using the **batch name**.
3. **Once the final task is completed**:
   - The **batch is marked as completed** → `status = completed`
   - A **callback is fired** to notify completion.

---

### **2️⃣ Skala Rapid Workflow**

For **real-time data processing**, Skala Rapid provides a more **structured** approach.

🚀 **Key Differences in Skala Rapid:**

- **Two types of batches:**
  - **Production Batches** → Used for normal labeling tasks.
  - **Calibration Batches** → Used to **test and validate** the labeling process **before full-scale production**.

**Process Flow:**

1. **Batch is created** → `status = staging`
2. **Tasks are added to the batch** → Specify the `batch` field.
3. **Batches must be finalized** → `status = in_progress`
4. **Once finalized:**
   - Tasks are **submitted for labeling**.
   - **No new tasks can be added** to the batch.
5. **When the final task is completed**:
   - The batch is **marked as completed** → `status = completed`
   - A **callback is fired** for confirmation.

---

### **3️⃣ Skala Studio Workflow**

For **internal team-based labeling**, Skala Studio ensures **controlled processing**.

**Process Flow:**

1. **Batch is created** → `status = staging`
2. **Tasks are added to the batch** → Specify the `batch` field.
3. **Batches must be finalized** → `status = in_progress`
4. **Once finalized:**
   - Tasks are **sent to your internal team** for labeling.
   - **No new tasks can be added** to the batch.
5. **When the final task is completed**:
   - The batch is **marked as completed** → `status = completed`
   - A **callback is fired** for confirmation.

---

## **Batch Object Example**

```json filename="batch.json"
{
  "project": "kitten_labeling",
  "name": "kitten_labeling_2020-07",
  "status": "staging",
  "callback": "https://example.com/callback",
  "created_at": "2020-07-01T09:09:10.108Z"
}
```

---

## **Batch Statuses & Definitions**

<div style={{ marginTop: "1.5rem" }}>
  <Table>
    <thead>
      <Table.Tr>
        <Table.Th>Status</Table.Th>
        <Table.Th>Description</Table.Th>
      </Table.Tr>
    </thead>
    <tbody>
      <Table.Tr>
        <Table.Td>
          <code>staging</code>
        </Table.Td>
        <Table.Td>Batch is created but not yet finalized.</Table.Td>
      </Table.Tr>
      <Table.Tr>
        <Table.Td>
          <code>in_progress</code>
        </Table.Td>
        <Table.Td>
          Batch has been finalized and is now processing tasks.
        </Table.Td>
      </Table.Tr>
      <Table.Tr>
        <Table.Td>
          <code>completed</code>
        </Table.Td>
        <Table.Td>All tasks in the batch are completed.</Table.Td>
      </Table.Tr>
    </tbody>
  </Table>
</div>

---

## **Summary of Workflows**

| **Workflow**   | **Purpose**                                                          |
| -------------- | -------------------------------------------------------------------- |
| **Enterprise** | Large-scale project management with ongoing submissions.             |
| **Rapid**      | Time-sensitive processing with **calibration & production batches**. |
| **Studio**     | Internal team-based task labeling.                                   |

> **Note:** Batches provide structured **task organization**, **workflow automation**, and **real-time tracking**.
